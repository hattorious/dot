- clean: [
    '~',
    '~/Library/Fonts'
]

- link:
    ~/.dotfiles: ''
    ~/.gitconfig: git/gitconfig
    ~/.gitignore_global: git/ignore_global

- shell: 
    - [source fonts/install_fonts, Installing fonts]
    - [git submodule update --init --recursive, Installing submodules]
